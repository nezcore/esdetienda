{
  "name": "@esdetienda/worker-api",
  "version": "1.0.0",
  "description": "API Worker para EsDeTienda - Endpoints con Hono",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --local --ip 0.0.0.0 --port 8787",
    "deploy": "wrangler deploy",
    "build": "esbuild --bundle --format=esm --target=es2022 --outfile=dist/worker.js src/index.ts",
    "test": "vitest"
  },
  "dependencies": {
    "hono": "^3.9.0",
    "@supabase/supabase-js": "^2.39.1",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20230922.0",
    "@types/node": "^20.0.0",
    "esbuild": "^0.19.0",
    "typescript": "^5.0.0",
    "vitest": "^0.34.0",
    "wrangler": "^3.0.0"
  }
}
